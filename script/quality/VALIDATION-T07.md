# T07 上游整合与兼容性验收

**Build13 完整构建、宿主测试、APK 与独立静态审查通过；最终30组功能执行、四份独立范围审查及最终汇总裁决均通过。**

## 本轮结果

| 项目 | 实际结果 |
| --- | --- |
| APK 源码 | `35ae32d3fdfc29db3860fca32644cdf680240c47` |
| 源码范围 | 495 项构建输入；相对完整 Build12 仅通知图标一项输入变化 |
| 后置宿主修复 | 源码已包含 `2d2359ca` 的两文件准备判定修复；本轮没有旧源码摘要例外 |
| Host11 | 107 项通过：quality 63、T04 13、T05 5、baseline 26；571 项输入前后及当前一致 |
| 完整构建、应用 JVM、构建逻辑、Lint | 838.092 秒 / Gradle 13分56秒；415项应用JVM实际执行，9项构建逻辑为UP-TO-DATE；Lint0、构建警告0 |
| APK、签名、资源、原生静态 | 通过；180项证据摘要一致，编译图标无主题tint；两应用各2项META-INF提示、测试APK0项 |
| API21 | 10组功能与独立范围审查通过；Android 5.0 实际通知图标可见，未再出现 Build12 的主题属性警告 |
| API35 | 17组功能与独立范围审查通过；整机日志13207项事件、49个critical ID和59张原图均保留 |
| ARM 转译 | 正式engine、T02与独立应用3组功能和独立范围审查通过；原始警告、critical事件与17张原图保留 |
| 发布 | 仅两份应用 APK 与完整递归源码包；测试APK和验证材料不上传 |

上游固定为 [`88b661a7`](https://github.com/osfans/trime/commit/88b661a7a8c19a53e4ac3601444028a8db45390c)，整合 11 项提交，合并点为 `b6263373`。Build13 相对父提交 `2d2359ca` 仅删除 `ic_trime_status.xml` 的 `?attr/colorControlNormal` tint；图标尺寸、路径、填充和变换不变。相对 Build12 的三个 Git 路径还包括已审查的两份宿主文件；495 项 APK 构建输入中只有图标变化。

构建前根仓库与递归子模块严格检查为空，且无构建或模拟器进程。源码和 Host11 独立报告 SHA-256：`c79e5827e8f31549d4795c9d74ffcd2e0df7f468e4e3582234a9aa323218f901`；Host11 重验记录为 `7c613ab2fadb75ec1c4ad8e4c11aad7db0efc5551fff3023b676129a4af7f479`。最终 30 组计划一次冻结为 `3b404a7b0730abd02effd913909b0fc70a6a0f8092d3c5ef54872fcbff9b8e70`，后续验收状态单独记录，不修改此选择。

| 本轮冻结产物 | SHA-256 |
| --- | --- |
| ARM64 应用 APK | `6a87af8d67f6283526f27a64b58a72867c02bb149b512f393eb232e57b52819c` |
| x86_64 应用 APK | `62dcad2df5d8383b9be00d227e1570851520a39034b8df00a74617c7ac04cbbb` |
| 测试 APK（仅本地） | `34c1012c4cbe7bd9cc06dca733d1eef9a4856e7becc3e12d2904c3dcf5f49ab4` |

完整构建实际耗时838.092437027秒，114项任务中38项执行、76项UP-TO-DATE；时间戳 `1789377386873`。42份应用JVM XML共415项、2份构建逻辑XML共9项通过，分别由本轮实际执行和UP-TO-DATE任务提供。表中APK摘要已从本轮产物重算；签名、原生、编译图标和共享资源独立静态均通过。

独立静态报告SHA `f78b3ded24a251e8c47ad9ed1358446b3a5d46b725f557fe1f8efb9a5732018c`，180项证据摘要均一致。两ABI通知图标实际编译条目同为1492字节、SHA `23917e408061e12217be05ababbd7fe9e99e62d1d8a993a1bb47918f36a57163`，无tint/主题属性，其他尺寸、路径、变换与填充与Build12精确相同；编译图标报告SHA `488ca3d6d6fa315c550b72e067435e9539fddf25c47902f79069209688973941`。实际两ABI原生报告SHA `cc8e0864e1a666ed3e7d4725ff147b4eb1708efde640bd787ceb59fc2b245814`，20快照及本轮链接/strip/APK运行段通过。上述静态结果不替代设备运行。

## Build13 设备验收

API21 的10组已完成并通过独立范围审查。setup 使用实际 Build13 PID 与通知记录验证 `ic_trime_status`，原图中可见白色 Trime 图标，当前范围没有 Build12 的主题资源解析警告。editors 完成输入框矩阵、52项字形、4次小数点触摸、4项气泡检查和连续100次显示/隐藏；其余组覆盖引擎、启动失败、反馈、关闭、剪贴板保存、独立应用、资源失效恢复和导航。API21全10组审查SHA为 `a59114645653282f809a31d218cf19e6276c976ebedd716fc9bbac3fc68c81af`，契约复核SHA为 `89e105d63c1cf4d50ecfdbf2feca8e86cf9cc5c2b86058a885f84e33b5a8be87`。

API35 的17组已通过各自独立范围审查。主范围保留并逐条分类731项W/E或critical事件，报告SHA为 `c4da2def6b27251edef69ec02355c4814be785febbc84979cccab9421794aa5c`，契约复核SHA为 `dd5d787e7b7aa4cf1dea6b07c3aa1edd983970d830aec177ea5b3c470c62aed8`。其中ClipSave结束窗口附近出现一组Android框架MessageQueue/Choreographer线程退出警告；源码与AOSP路径支持窗口移除时框架线程收尾的解释，未出现抛出异常，但实际线程所有者及最终quit调用未捕获，因此保留原文和这一边界。外部7组保留13207项整机事件、49个critical ID、207项项目事件和59张原图；12819项非项目整机事件由早、中、晚三段逐项接受，完整分段复核SHA为 `c1e2d798824af204bc84fe2a894e704e46912937a4f55640ee592e19617cbd1a`，绑定宿主可靠性收据后的外部7组最终审查SHA为 `5655e0297aaf6e104826ad4b94c298aa7dd07c368d6eb16c6abb56a84a7865b0`。接受表示每项有受限的所有者/上下文判断，不表示日志无警告或所有平台问题均无害。

ARM64 APK 在Google API35 x86_64模拟器上通过 `libndk_translation.so` 执行。公开t9.8旧版准备先完成存储、输入法选择和向导DONE，随后旧版PID3718在Main导航建图时产生ANR（ErrorId `a587fa5f-346f-46f7-bb5b-1ab64f735267`）；一次实际“Wait”没有清除对话框。强制停止该旧进程后，新PID7979已启动，但该次命令与日志记录器均返回255，原尝试保持失败。随后在同一任务和同一旧APK上不再重启，取消通知提示并连续观察Main超过10秒，建立0字节专用标记，独立审查确认旧基线可用于升级。这个恢复链不把旧ANR称为已修复，也不把返回255的原因归因于未证实的传输故障；旧版中文输入未作为准备判定测试。

从旧t9.8 ARM64 APK `0587308c…028f51aa` 覆盖安装Build13后，应用、测试与两份fixture身份均匹配，53字节私有升级标记保留。旧基线恢复链独立审查SHA为 `de48a3d84dc5cf845b65233e81787979c1149656567c99f3e66f3e894d201653`。额外冷Main导航回归在Build13新PID8792上功能通过且没有项目诊断；它不计入冻结的30组。

正式ARM环境的第一次新准备在观察器等待期间收到KeyboardInterrupt，日志记录器随后以-2退出，证据不完整，保留为失败。retry1建立了连续launcher观察并安装精确Build13，但没有完成应用存储/向导准备；其proof字段仍为false。紧接的engine PID5554完成7项协程封装检查后因 `Rime storage is unavailable` 失败，因此不计入最终30组，也不能据此判定Build13引擎缺陷；字节保留的迁移收据SHA为 `b97624753d627ae9264cb5097c97554c3edc940a307862627c927e3b40e64c3c`。随后通过实际界面选择应用专用存储、依次完成Step 1/2/3、打开可见输入法选择器、选择Trime、点击DONE、取消通知提示，并连续观察Main 10秒；0字节专用标记建立，准备报告通过，SHA为 `a0a69eeafb66049d7abd181a928f058e3b8a6753a8fb0c881c0f16e86684fac9`。

有效准备后的第二次engine尝试又在instrumentation之前被runner以“已安装测试APK摘要不匹配”拒绝。runner没有保存当时读取到的实际测试APK字节或摘要，后续匹配读数无法重建失败输入，根因保持未知；该失败也不计入最终30组，迁移收据SHA为 `f763c1907f3e29402191174deeb49483ac045bd2ee0edb7b9dcca0e09646ff28`。第一版稳定性复核又错误要求run_batch切换后的默认输入法仍为Trime，虽已在内存中读取数据但未输出报告；修正版保留此前脚本并按实际LatinIME上下文执行。随后应用与测试APK各5次完整读取均匹配冻结摘要，复核SHA为 `945bcb9db07bdfbade3ef8e42029c39dcd5c9a374c433fcddc78ff96d2f52d11`；它证明当前字节稳定，但不恢复或消除先前未保存的失败读数。

在这组稳定身份上，正式engine第三次以PID7264通过，含7项协程检查、既有JNI/协程路径和32次OpenCC损坏配置句柄恢复检查；正式T02以PID7386通过。两组原始报告SHA分别为 `ccbb09a5c8f6a6de137781db75b9a394181d6496205721612795c416d665ff57` 与 `678e85c1b61cf06f240c26fd89d4ea265ccb6554c24c85b7c93dc2ff4a80c7f3`。随后以实际界面选择朙月拼音九键，正式独立应用组由PID7521与8132完成17项检查点，覆盖杀进程恢复、旋转、应用切换、地址/邮件/密码/电话/整数/小数输入框及返回聊天框九键恢复；功能通过且没有已知项目诊断。

ARM三组独立范围审查逐项接受101条应用W/E、14条项目事件、48个critical ID/48次出现，并查看17张原图；未解决项为空，`warning_free` 保持false。13项SystemUI、Phone或GMS启动/准备ANR及3项相关SystemUI ANR窗口生命周期记录作为真实的探针前平台事件保留；26条Binder `-22` 只确认异步目标为 `0:0`，不能识别发起的Java/native调用方或接口；3条 `/dev/pmsg0` SELinux拒绝的原生调用方也未知。最终ARM审查SHA为 `20f92027e155a573020d21e210b896fc540da9647ef214be985aa0e0acf90436`。它只接受这三组边界内证据，不扩张为物理ARM、原生ARM镜像、分屏、无警告或所有平台问题均无害。

首份ARM审查 `daa5b4b5755df447d3fb4b36cebeff37cd085b4de75f83a6d6c529c1d90123ac` 的证据结论相同，但14条项目事件没有汇总契约要求的顶层 `event_id`，首次最终汇总因此以rc1拒绝且没有生成裁决。该审查完整保留为契约失败历史；retry1仅把既有稳定 `id` 显式绑定为 `event_id`，并作为上述最终ARM门。

冻结汇总实际选择30/30组，证据检查通过、错误0项，保留166个需要逐项裁决的critical finding，`warning_free` 为false；其SHA为 `fd8a7a9f3a4d565a4c6995299a982ce06c5939ddc7914e70ccaa5d311b67ca62`。汇总命令返回rc2是 `individual_critical_review_required` 门的预期结果，不是功能执行失败。最终裁决绑定API21十组、API35主十组、API35外部七组和ARM三组四份审查，逐项覆盖166个critical ID/188次原始出现和647个项目作用域索引；未匹配critical ID、未审项目诊断行、无法解释的发布阻断诊断和其他未解决项四类均为空，运行发布门通过。裁决固定1866项文件摘要；独立全量重算829692202字节没有不匹配。裁决SHA为 `717b6b1e0daa80a41bd8b1b65cd1a376d885ff7b0d4aa7b1b8abfe0d005ca463`，实际聚合rc0且stderr为空，执行收据SHA为 `f23c7393765cf8a73f6b3f86ba06aeed99af41877e3937b27a03d767faf4ff3d`。裁决保留原始汇总的rc2、`warning_free=false` 与逐项审查要求，不把接受改写成无警告。

## 宿主证据读取异常与边界

证据处理中保留五项宿主/读取异常：Build12首次原生capture末尾摘要不一致；Build13外部日志首次序列化遇到超过Python默认4300位限制的整数；随后两次读取已保存事件JSON时分别在字符28085838和45804119处报告解析错误；正式ARM第二次engine尝试在instrumentation前报告测试APK安装摘要不匹配。最初不一致的expected摘要、超长整数、两份失败字符串，以及ARM当次实际测试APK字节/摘要均未保留，因此根因未知，不能称为已修复，也不能归因于未经证明的硬件、索引器、传输或Python问题。

当前固定快照另行完成交叉验证：Node、jq与Python各连续3轮解析5份JSON，逐字节UTF-8往返一致；13207项事件、207项项目事件、49个critical ID/70次出现、224392项上下文与16份原始日志逐行重算相符。495项构建输入、571项宿主输入、180项静态证据和3份APK共755个实际文件、89443137字节也由Node流式重算匹配。由此只接受SHA固定的当前数据继续语义审查，不扩大为宿主对任何间歇故障均可靠。根评估记录SHA为 `9661477ae532b7785cf7ea95446c9f6ba700f1b7854dd14e120f83a9e0da78d3`；它保留历史异常，后续独立审查只裁决当前固定快照，不反推历史异常原因。

## 主要修复与检查范围

- 主题与按键：整合键盘默认值、几何继承、动作解析和源配置 DSL 读取；保留本分支九键、回车、长按和滑动行为。不支持的 DSL 正常转入原生部署时仅记录说明，真实损坏 YAML、读取和部署失败仍报告并可恢复。
- 编辑器与气泡：输入类型切换后恢复九键，隔离编辑器状态；气泡与长按键盘随配色更新，保留焦点、几何和对象复用。旧主题缺失现代配置时回退尺寸、字号和字体；随包主题明确使用系统预览字体。图标组件适配 IME 主题。
- 降级输入：大字体下保留原按键大小，修复数字、“@”和小数点裁切。真实失败与 Retry 单独测试，不把可视化构造的 Failed 状态当成引擎故障。
- 引擎与词典：停止/重启时取消旧上下文提示任务；转换成功且校验后再替换文件，失败保留旧输出。修复 OpenCC 异常路径缓冲及文件句柄释放，缓存只复用输入和输出摘要均未变的转换结果。
- Android 5.0 通知：删除状态图标对宿主主题属性的依赖。需要同时核对实际 APK 的编译 XML、通知运行日志和原图，不能仅凭源码宣称设备问题已消除。

最终设备执行先 API21，再 API35，最后 ARM 转译。API21 从 setup 开始尽早验证通知；之后 editors、engine、startupFailure、feedback、shutdown、clipSave，以及独立应用、资源失败恢复和导航。API35 除主十组外，包含横屏大字号布局、剪贴板可见性、完整 T03/T05、独立应用、资源失败恢复与导航七组。ARM 转译覆盖 engine、T02 和独立应用三组。旧版基线、覆盖升级、环境/界面准备、T9可见选择和额外冷Main回归均不计入最终30组；所有失败准备仍单独保留。

两个 editors 必须实际完成输入框矩阵、四项气泡配色检查、52 项字形与四次小数点触摸、连续 100 次显示/隐藏。三个 engine 必须由本次真实 PID 输出既有 JNI/协程检查点，以及 32 次损坏 OpenCC 配置字典的句柄关闭和同路径恢复检查。独立应用覆盖进程恢复、旋转和应用切换；真实分屏输入由API35 x86_64范围覆盖，ARM独立应用组不含分屏。资源不可用组覆盖降级输入、资源恢复和 Retry。

升级来源为 API21/API35 实际 Build12 x86_64 应用 `16c98c80…d4fb11b2` 与 ARM 公开 t9.8 `0587308c…028f51aa`，须核对旧包、新包及 53 字节私有标记保留。旧候选、追加诊断和宿主原生对照均不替代本轮设备结果。

## 证据与验收规则

完整构建以原始日志、JVM XML、任务执行状态和 Lint XML 为准；资源修改可能使部分任务为 UP-TO-DATE，报告如实区分。48 项共享资源需要与固定 Build12 完整映射逐项比较，再对实际 APK 全部条目重算；生成的词典资源不误记为原始源码直接复制。

通知图标另设两 ABI 的实际编译 XML 门槛：新 APK 无 tint 或主题属性引用；与拒收 Build12 的编译 XML 相比，仅移除原 tint，其余尺寸、路径、变换和填充值一致。只忽略 aapt2 的源码行号注解，APK、编译条目、工具与原始输出均绑定摘要。

原生审查继承 Build7 已冻结的 30 项未变输入、20 份快照、五处精确 RAII 替换、编译命令、对象及 OpenCC 归档成员，另核验本轮真实 JNI 链接、strip 和 APK 运行段。对象复用不称为新编译。此前八项宿主原生对照（四个原版、四个修正版，各 32 次）只证明明确场景，既不计入 Host11 的 107 项，也不代替 Android 运行。

四份独立范围审查覆盖精确30组。最终裁决要求每个critical ID的目录、类别、原文和全部file/line/scope occurrence匹配；项目诊断按每目录、案例、作用域和序号逐条覆盖真实行号，重复原文仍保留独立位置。tested-IME子范围和整机日志分别核验，不合并或隐去；故障注入、平台PID或早期日志都不自动豁免。未解决项会阻止发布，原始警告与汇总退出状态保持；本轮上述四类未解决集合均为空。

## 兼容性与限制

保留 Android 5.0+、ARM64、x86_64，min API21、target API37、`com.osfans.trime.debug`、原证书与 debuggable 变体。实际模拟器范围为 API21/API35 与 ARM 原生桥转译；不宣称物理 ARM、OEM 或 16KB 页设备实测。ELF/ZIP 16KB 静态对齐与设备页大小验收分别记录。

协程异常处理与主线程调度所需 META-INF 服务保留，本轮两应用各保留2项META-INF签名提示，测试APK为0项；不宣称所有警告或风险已消除。词典异常/句柄回归不等于任意恶意输入安全性、Android ENOSPC 或断电持久性保证。模拟器耗时与 PSS 是本次观测，不是物理设备性能保证。横屏大字号下部分对话框内容可能滚动；可见性、Back/Cancel 与实际输入范围按原图和动作记录，不扩张为未测试的保存行为。

Release 仅上传 ARM64 应用 APK、x86_64 应用 APK 和包含全部固定子模块的递归源码包；保留 GitHub 自动源码入口。测试 APK、生成报告、校验清单、身份 JSON 和 validation ZIP 不作为附件。归档只包含经核对的已提交 Git 文件，不向源码包注入生成的运行证据；原始记录与完整历史草稿留在本地。

发布前清理已将现有7个Release的28个辅助附件备份、校验并删除，保留10个APK和3个完整源码包；三个受影响的Release正文随后改为仓库内标签文档链接。删除验证SHA为 `6afa2dc44c6888e63a436be46af0e8f3f0aeab5ff4a0580d0319157f2d83585a`，正文修复验证SHA为 `198c77ecf3375872a32a4ab69062420ecff07fe88953cc4c71e9de683aeb5ca4`。Build13发布后仍须重新枚举全部Release，核对旧资产ID/名称/摘要未变、新Release恰有3个上传附件，并分别验证稳定Latest与下载摘要。

<a id="candidate-history"></a>

## 候选历史

下表保留拒收原因与修正脉络；所有旧候选的运行对本轮最终 30 组贡献为 0。每轮完整草稿、原始失败及工具改动前副本已分别保存，避免用后续通过覆盖失败。

| 候选 | 结果与后续修正 |
| --- | --- |
| Build6 / `02327713` | 完整构建通过，源码审查发现可达 OpenCC 资源泄漏，未进入设备验收；随后加入异常路径 RAII 修复及原生正反对照。 |
| Build7 / `c28a5d10` | 静态通过并冻结原生证据；设备发现缺失气泡默认值导致零尺寸，以及图标组件主题不兼容。后续恢复默认值并改用框架 ImageView。 |
| Build8 / `870a7f87` | 源码审查发现默认字体引用未提供的 latin.ttf，受控停止，完整构建/Lint未完成；随包字体改为空列表，使用系统字体。 |
| Build9 / `42137583` | 静态通过；A→B 气泡图标边缘像素断言失败。实际诊断与量化差异相符，独立参考图对照通过，改用独立 Iconics 全像素参考及旧配色负例；另补初始已服务编辑器等待。 |
| Build10 / `aaaa1437` | 发现上游新增三个主题 DSL/读取提交后受控停止，未完成完整验收；随后整合至固定上游88b661a7。 |
| Build11 / `b6263373` | 静态与编辑器功能断言通过，但正常 DSL 回退产生两次各23行警告堆栈，共46条项目诊断，拒收。后续只将正常分支降为单行 INFO，保留真实错误警告。 |
| Build12 / `3ed10ad8` | 静态通过；API21/API35功能检查完成，但通知触发实际状态图标主题属性警告，拒收。ARM追加检查仅作诊断。随后仅删除该图标 tint，并重新构建和跑全30组。 |

Build12 另保留两项重要历史：

1. API21 原 editors 和 diagnostic1 因宿主准备判定过严失败。系统实际 ACTION_SEND 携带 NAVIGATE_NEXT；宿主提交 `2d2359ca` 仅改驱动和测试，113 份原始观察重放中接受111份，最初2份真实未准备状态仍被拒绝。正式 retry1 通过，失败记录不删除；该宿主修改已包含在 Build13 源码中。
2. 首次原生 capture 在末尾 x86_64 输出稳定性检查失败，未记录当次 expected 摘要，原因未确认。相同未改脚本的带trace重试、五次连续 SHA/stat、外部 sha256sum及随后独立两轮核验均支持冻结输出；不能反推首次失败原因。

| 历史证据 | SHA-256 |
| --- | --- |
| Build7 原生冻结 | `033fb29d7a60c75ea8f1829d99d0a6b748b21d55e892632c765fdaea3bfd2323` |
| 图标独立参考诊断（旧 Build9 应用） | `9f68e828227670f029574430ddaab386fc8db2b008e67e27e217dc7ef707d7cc` |
| Build11 追加引擎/启动失败诊断 | `eed92550984791cdfb15b5308bfdca9429936dff9a946feb9a0665a9c4732ce7` |
| Build12 独立静态 | `b8e9028d8d513958250fbbcb3b30eec735bb1cceb14c5a4c32ad6c4944daf416` |
| Build12 原生 capture 首次失败后复核 | `2017a72b11e60cba17c7b271cf829d4860d2886122462094c177ce967f0a9716` |
| 两文件宿主修复迁移 | `67636711f008f2c4f5d28b818464279a1b31defec6e135390ebc1b1a19cbdc8f` |
| Build12 API21 完整审查（拒收） | `05f337a652f0bd1674f2e91afb7d1170d5c38e6ff67e24c45b2ca52f09e2842f` |
| Build12 实际编译图标基线 | `c9c3066a9d6d73235cef2bbd1faf48b9ca4759e2109719b59bec8069c3f03cbd` |
